[package]
name = "cksol_minter_mock"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10.22"
currency = { path = "../../.." }
ic-cdk = "0.20.0"
ic-cdk-macros = "0.20.0"
serde = { version = "1.0.227", features = ["derive"] }
